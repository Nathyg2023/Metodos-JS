//Devuelve el menor de los números dados cómo parámetros de entrada o "infinity" sino hay parámetros.

const numbers = (num1, num2, num3) => {
    return Math.min(num1, num2, num3)
}

console.log(numbers(2, 8, 10))
console.log(numbers(-1, -3, -2))

