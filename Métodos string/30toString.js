/*Método .toString:

Este método devuelve una cadena como una cadena, no cambia la cadena original, se puede utilizar para convertir un objeto de cadena en una cadena.*/

const x = new String("Hello world");

console.log(x.toString());

const stringObj = new String('foo');

console.log(stringObj);
console.log(stringObj.toString());


