/*Método .keys():

Este método devuelve un objeto Array Iterator con las claves para cada índice de la matriz*/

const array1 = ['a', 'b', 'c'];
const iterator = array1.keys();

for (const key of iterator) {
  console.log(key);
}











